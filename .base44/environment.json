{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_SUPABASE_URL",
      "description": "Supabase project URL (optional — enables cloud auth/sync; app runs in local-only mode without it)",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_SUPABASE_ANON_KEY",
      "description": "Supabase anon public key (optional — enables cloud auth/sync; app runs in local-only mode without it)",
      "requiredAtBoot": false
    }
  ]
}
